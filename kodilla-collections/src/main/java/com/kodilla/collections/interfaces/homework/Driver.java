package com.kodilla.collections.interfaces.homework;

public class Driver {
    //flatmap

//    List<Driver> driversList = Arrays.asList(
//            new Driver("Karol", Arrays.asList(
//                    new Car("Kia", "Red"), new Car("Kia", "Blue"))
//            ),
//            new Driver("Kuba", Arrays.asList(
//                    new Car("Ford", "Black"), new Car("Fiat", "Yellow"))
//            ));

//driversList.stream().flatMap(person -> person.getCars().stream())
//            .map(car -> car.getName())
//            .distinct().collect(Collectors.toList());
}
